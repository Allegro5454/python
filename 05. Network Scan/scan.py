#!/usr/bin/env python3
#mport argparse
#mport socket
#arser = argparse.ArgumentParser()
#arser.add_argument("--IP","-i",help="helps with scanning a specific IP address",required=True)
#arser.add_argument("--verbose","-v",help="Shows additional information",action='store_true')
#arser.add_argument("--ports","-p",help="specifies the ports to scan, default is 1-7 65535",default="1-65535")
#rgs = parser.parse_args()  
#
#outer = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
#outer
import random
a = random.randint(1, 3)
print(a)